Access DB in container:
docker exec -it db_db_1 psql -U xccup_user xccup_db

Working with timezones in psql:
SHOW TIMEZONE;
SELECT * FROM pg_timezone_names; 
SET TIMEZONE='Europe/Berlin';

Enable PostGIS Extension:
https://stackoverflow.com/questions/6850500/postgis-installation-type-geometry-does-not-exist
docker exec -it db psql -U xccup_user xccup_db -c "create extension postgis;"
docker exec -it db psql -U xccup_user xccup_db -c "create extension postgis;" -c "set timezone='Europe/Berlin'"

HowTo Connect PgAdmin to Postgres:
Add New Server -> Connection -> Hostname: <<name_of_postgres_container>> Port: 5432
Localhost as address doesn't work because PgAdmin runs in his own host!

Compile C Code in linux
gcc olc2002.c -o olc_lnx -lm